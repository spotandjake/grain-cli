# TODO

* Once deno `v2.8.3` is released drop fs shim
* Once `oscar/oos` is merged simplify stdlib resolution / setup
* Test `--debug` && `--verbose`
* Make a basic test suite
* Merge in with grain
* Look further into switching to cliffy from commander.